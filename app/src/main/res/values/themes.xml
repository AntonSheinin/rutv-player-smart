<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme with RuTV logo -->
    <style name="Theme.VideoPlayer.SplashScreen" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Base theme for Compose-based app -->
    <!-- Actual theming is handled by Compose Material3 theme in ui/theme/Theme.kt -->
    <style name="Theme.VideoPlayer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Theme wrapper used to force texture-backed PlayerView instances -->
    <style name="Theme.VideoPlayer.PlayerViewTexture" parent="Theme.VideoPlayer">
        <item name="surface_type">texture_view</item>
    </style>
</resources>
