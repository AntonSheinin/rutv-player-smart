# AGENTS.md

This repo uses the `rutv-stb-deploy` Codex skill for the environment-specific Android build, APK install, launch, startup timing, crash-log, and ADB profiling commands.

Use the skill when asked to build or deploy the app to the configured STB:

```text
$rutv-stb-deploy
```

Skill location:

```text
C:\Users\anton\.codex\skills\rutv-stb-deploy\SKILL.md
```
